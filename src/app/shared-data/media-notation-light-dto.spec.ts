import { MediaNotationLightDto } from './media-notation-light-dto';

describe('MediaNotationLightDto', () => {
  it('should create an instance', () => {
    expect(new MediaNotationLightDto()).toBeTruthy();
  });
});
